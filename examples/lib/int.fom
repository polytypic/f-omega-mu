let Target = import "target"

let compare = λl: int.λr: int.
  if l < r then
    'Less
  else if l =[int] r then
    'Equal
  else
    'Greater

let to_string = Target.to_string[int]

{compare, to_string}
