[(∀α.α → α) → ∀α.α → α, (∀α.α → α) → ∀α.α → α, (int → int) → int → int, (int → int) → int → int, (int → int) → int → int, (int → int) → int → int, (∃α.α → α) → ∃α.α → α, (∃α.α → α) → ∃α.α → α, (∀α.{b: α, a: int, c: string}) → ∀α.{b: α, a: int, c: string}, (∀α.{b: α, a: int, c: string}) → ∀α.{b: α, a: int, c: string}, (∀α.∀β.{a: α, b: β}) → ∀α.∀β.{a: α, b: β}, (∀α.{a: α, b: α}) → ∀α.{a: α, b: α}, (∀α.{a: α, b: α}) → ∀α.{a: α, b: α}, (∃α.{empty: α, concat: int → int → int}) → ∃α.{empty: α, concat: int → int → int}, (∃α.{empty: α, concat: α → α → α}) → ∃α.{empty: α, concat: α → α → α}, {x: μτ.(int, τ), y: μτ.(int, τ)} → {x: μτ.(int, τ), y: μτ.(int, τ)}, (∃α.{x: μω.(α, ω), y: μω.(α, ω)}) → ∃α.{x: μω.(α, ω), y: μω.(α, ω)}]
